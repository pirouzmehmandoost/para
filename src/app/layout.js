import { Nanum_Gothic_Coding,  UnifrakturCook} from "next/font/google"
import "./globals.css";

const nanum = Nanum_Gothic_Coding({ weight: '400', subsets: ["latin"] });
const cook =  UnifrakturCook({ weight: '700', subsets: ["latin"], });
export const metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body className={cook.className}>{children}</body>
    </html>
  );
}
